import { CustngifDirective } from './custngif.directive';

describe('CustngifDirective', () => {
  it('should create an instance', () => {
    const directive = new CustngifDirective();
    expect(directive).toBeTruthy();
  });
});
